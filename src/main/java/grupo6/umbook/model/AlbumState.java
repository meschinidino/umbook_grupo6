package grupo6.umbook.model;

public enum AlbumState {
    VACIO,      // El álbum no tiene fotos.
    CON_FOTO,   // El álbum tiene al menos una foto.
    ELIMINADO   // El álbum ha sido borrado lógicamente.
}
